#!/usr/bin/env bash
# Run composer commands
# Usage: bin/composer install

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR/docker"
docker compose exec phpfpm composer "$@"