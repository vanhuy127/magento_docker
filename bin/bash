#!/usr/bin/env bash
# Open bash shell inside the container
# Usage: bin/bash

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR/docker"
docker compose exec --workdir /var/www/html phpfpm bash "$@"